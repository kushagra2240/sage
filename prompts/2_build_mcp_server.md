"In mcp_server/server.py, build a FastMCP server called 'sage-tools'. Add three tools: web_search(query: str) -> list[dict] that calls the Tavily API and returns results with title, url, and snippet; extract_content(url: str) -> str that fetches and extracts the main text content from a URL using httpx and BeautifulSoup; and save_note(key: str, content: str) -> str that stores content in a simple in-memory dict and returns a confirmation. Load the Tavily API key from environment variables. Add proper error handling and docstrings to each tool."