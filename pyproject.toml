[tool.ruff]
line-length = 100
target-version = "py310"
exclude = [".venv", "content", "prompts"]

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
